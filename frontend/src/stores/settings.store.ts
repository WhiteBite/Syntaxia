/**
 * Settings Store - Re-exported from core.store.ts for backward compatibility
 * @deprecated Import from '@/stores/core.store' instead
 */

export {
    useSettingsStore, type AppSettings, type ContextSettings, type ContextStorageSettings, type FileExplorerSettings, type OutputFormat, type QuickFilterConfig
} from './core.store';

